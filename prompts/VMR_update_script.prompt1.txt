Write a new python program, called vmr_update_sql.py.
Like all other programs, please use the argparse and pandas packages. 
The default output directory will be the filename of the input VMR...xslx file, without the .xlsx. A command-line argument can over-ride this default.
The program should write out an "errors.xlsx" file before it exits, in the output directory, reporting on all the errors found (which should also be reported to stderr). In the xlsx, each line should include the filename, worksheet name and row that is generating the error, plus a statement of what the problem is. 


The program will load an excel file, emailed from the VMR editor, by default, "./VMRs/VMR_MSL40.v1.20250307.editor_DBS_22 July.xlsx".
Validate that there is a worksheet named "VMR MSL[0-9]+". That tab contains the "updated" records.
Validate taht there is a 2nd worksheet named "Original". That contains the "original" data records, and should match the contents of the MariaDB SQL databases "ictv_database".

Validate that both worksheets exist, and throw a hard error if they dont.
Validate that the updated records has data in columns A:AG, and that the headers match the example I have attached (create a hard-coded list of required columns from the attached spreadsheet). 
Validate that "Original" exists, also with headers in A:AG, as shown in the upload.

If either worksheet is missing, or has incorrect header rows contents, issue an informative, human-readable error, and exit. For example, enumerate which column headers are missing, if some are.

After that, in default mode, the program will exit at the first error. If the -k flag is given, then it will continue processing and will list all errors.

Once loaded, compare the records in the two sheets, matching them using the value in column A "Isolate ID".
For each record in "Original" there should be one and only one record in "updated".
Any "Original" record matching more than one record in "updated" should create an error, which clearly states which rows in updated match which row in "Original".

There may be rows in the "updated" worksheet that are blank in column A "Isolate ID". Those are new records to insert into the database.

The program should create two output .sql files, using MariaDB style SQL, with the default filenames: vmr_1_updates.sql and vmr_2_inserts.sql.
   The comments in those SQL files must include the name of the input VMR...xlsx file and the date they were generated, as well as the version of this script (from version_git.txt)
   "vmr_updates" will have SQL update statements, one for each record, that will update just the cells that differ between "updated" and "original", settting them to the "updated" values in the database, if they aren't already set to that.
   "vmr_inserts" will insert the records in "updated" that are new (are blank in "Isolate ID").
The table to insert and update is "species_isolates", the MariaDB create table statement of which is also attached.
Before generating the SQL, the program should sanity check the data, assuming that all updates are done before any inserts: 
       1. no new records should duplicate any existing record's accessions. 
       2. there can only be one row with
Not all columns can be updated. Read-only columns include: [
    A: Isolate ID
    B: Species Sort
    D: Realm
    E: Subrealm
    F: Kingdom
    G: Subkingdom
    H: Phylum
    I: Subphylum
    J: Class
    K: Subclass
    L: Order
    M: Suborder
    N: Family
    O: Subfamily
    P: Genus
    Q: Subgenus
    S: ICTV_ID
    AB: Accession Link
]
Changing the value of a read-only column should be an error.

The columns in the "udpated" worksheet from AD: "QC_Status" and afterwards should be ignored.

The uploaded example contains formulas in the sheet "SQLupdates", in row 2, that generate MSSQL style SQL update statements, and excel formulas in sheet "SQLinserts", in row 2, that generate MSSQL style SQL insert. Use those as templates for writing the python code that will generate MariaDB style SQL. Do not update values that have not changed. Those excel formulas can also be used to extract the column name mappings to the SQL table. 


